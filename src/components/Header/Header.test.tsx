describe('<Header />', () => {
  it('should render <Logo />', () => {
    expect(false).toBe(false);
  });

  it('should render "My Orders" <Link to="/favorites" />', () => {
    expect(false).toBe(false);
  });
});
